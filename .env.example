OPENAI_API_KEY=sk_test_...
OPENAI_AGENT_MODEL=gpt-4o
OPENAI_GRADER_MODEL=gpt-4o-mini
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# LangSmith observability (free) — auto-traces every LLM/agent/graph call.
# Sign up at https://smith.langchain.com, then Settings → API Keys.
LANGCHAIN_TRACING_V2=true
LANGCHAIN_API_KEY=
LANGCHAIN_PROJECT=hiregraph
LANGCHAIN_ENDPOINT=https://api.smith.langchain.com

MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/
MONGODB_DB_NAME=recruiting_pipeline

PINECONE_API_KEY=pcsk_...
PINECONE_INDEX_NAME=hiregraph

# Google OAuth — sign-in only (non-sensitive scopes, no verification needed)
GOOGLE_CLIENT_ID=...apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=...
GOOGLE_REDIRECT_URI=http://localhost:8000/auth/google/callback

# Gmail SMTP — sends offer letters and interview invites.
# Uses an App Password (not OAuth), so no Google verification is required and
# it can email any recipient. Enable 2-Step Verification, then create one at
# Email is sent via the Brevo HTTP API (cloud hosts like Railway block SMTP ports).
# 1. Sign up free at https://www.brevo.com (300 emails/day)
# 2. Verify GMAIL_ADDRESS as a sender (Senders & IP → Senders)
# 3. Create an API key (SMTP & API → API Keys) and paste it below
GMAIL_ADDRESS=youremail@gmail.com
BREVO_API_KEY=
EMAIL_SENDER_NAME=HireGraph

TAVILY_API_KEY=tvly_...

FRONTEND_URL=http://localhost:3000
